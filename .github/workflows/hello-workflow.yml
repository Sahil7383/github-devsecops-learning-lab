name: Hello Workflow

on:
  push:

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Greeting
        run: echo "Hello GitHub Actions 🚀"

      - name: Show Current Directory
        run: pwd

      - name: Show Files
        run: ls -la
